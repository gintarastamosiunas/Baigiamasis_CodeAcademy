# GraphQL + React Event Events API

# Usage

Install all dependencies
```sh
npm install
```
BACKEND START
Install latest node js version

Install mongodb 
```sh
https://www.mongodb.com/docs/manual/installation/
```

Run the server
```sh
npm start
```
